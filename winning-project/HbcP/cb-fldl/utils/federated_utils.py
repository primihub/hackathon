def train(model, data):
    # 使用训练数据更新模型参数
    # 返回更新后的模型
    updated_model = ...
    return updated_model

def evaluate(model, data):
    # 在测试数据上评估模型性能
    accuracy = ...
    return accuracy
